# Use PyTorch CPU-only wheel index — prevents pip from pulling 2 GB of NVIDIA CUDA
# libraries that are useless on a CPU-only API server.
--extra-index-url https://download.pytorch.org/whl/cpu

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
pydantic-settings==2.6.1
pydantic>=2.0
email-validator==2.2.0

# Redis
redis[hiredis]==5.2.0

# ML
xgboost>=2.0
scikit-learn>=1.4
joblib==1.4.2
numpy
pandas

# RAG — Ollama local (dev) or Groq cloud (production)
langchain==0.2.16
langchain-community==0.2.16
langchain-groq==0.1.9
faiss-cpu==1.9.0
sentence-transformers==3.3.1
pypdf

# HTTP client for n8n webhook triggers
httpx==0.27.2

# File uploads
python-multipart==0.0.12

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
